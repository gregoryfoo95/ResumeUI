export default class codeComponent {
  code: string;
  color: string;
  prefix: string;

  constructor(code: string, color: string, prefix: string) {
    this.code = code;
    this.color = color;
    this.prefix = prefix;
  }
}
